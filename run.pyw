"""
Double-click this file to launch Posture Monitor without a terminal window.

The .pyw extension tells Windows to use pythonw.exe, which has no console.
The app runs entirely in the system tray — left-click the icon to open it.
"""
from posture_monitor import main

main()
